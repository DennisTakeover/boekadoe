fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg2-binary>=2.9   # only needed if DATABASE_URL points at Postgres (e.g. via docker-compose)
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0

# Instagram (private/unofficial API — see app/adapters/aiograpi_adapter.py)
aiograpi>=0.4
pyotp>=2.9          # only needed if INSTAGRAM_TOTP_SEED is set

# Niche classification
anthropic>=0.40

# Country heuristic
langdetect>=1.0.9

# Optional: Google Sheets export (only needed if you use POST /runs/{id}/export/sheets)
gspread>=6.1
google-auth>=2.34

pytest>=8.3
httpx>=0.27
